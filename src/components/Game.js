import React from "react";
import Board from "./Board";

const Game = () => {
  const onClick = () => {};
  const jumpTo = () => {};
  const renderMoves = () => {};

  return (
    <div>
      <Board onClick={onClick} />
    </div>
  );
};

export default Game;
